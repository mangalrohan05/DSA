# Containers.cpp:

In this learning about:
- **Pairs**: storing tuple of integers

- **Vectors**: Similar to ArrayList

## Vectors :
syntax:
> vector'<'datatype> variable;

methods:
<pre>
x.push_back(val) //pushes value at end

x.emplace_back(val) //same as push_back but faster

vector<int> x(size, vals) -> {vals, vals, vals... size times} 
</pre>
